# 神翼面板 · 使用说明

神翼面板把 ToolDelta（神翼定制版）从 CLI 改造成浏览器卡片式控制面板：在原本不支持插件 / 模组的租赁服上接入机器人托管能力，并用 Web 界面完成查看、命令、玩家、插件等日常管理。底层框架逻辑未改，`web_panel/` 是新增的桥接层 + Flask 后端 + 前端。

## 一、启动方式

```bash
python web_run.py --host 127.0.0.1 --port 5101
```

浏览器打开 **http://127.0.0.1:5101/login**，用启动时终端打印的账号密码登录（凭据保存在 `web_panel_data/credentials.json`，删除该文件重启即可重置账号）。

> 需要 Python 3.10+（本项目用 Python 3.12 实测通过），依赖见 `pyproject.toml`。

Docker 方式见仓库 README（数据挂载 `/data` 可持久化）。

## 二、首次使用（三步）

1. **设置页 → 启动配置**：选择接入方式（NeOmega / FateArk / TanGame 等），按原版方式填服务器号（或山头码）、密码、验证服务器地址；fbtoken 可粘贴保存或放到运行目录。
2. **控制台 → 启动服务**：机器人自动连上你的服务器并开始工作。
3. **市场页**：挑选插件 / 整合包一键安装，给服务器添加原本没有的功能。

> 未完成配置时点「启动」会被拦截并引导到设置页，不会在后台卡死。

## 三、界面模块（七个页面）

| 页面 | 说明 |
|---|---|
| 控制台 | 机器人状态、启动 / 停止、快捷入口、运行日志 |
| 消息 | 与服务器对话：发公告、聊天、执行命令，回应实时回流 |
| 玩家 | 玩家列表与详情（能力 / 背包 / 坐标）、踢出 / 给物品 / 传送 / 封禁、内置黑名单 |
| 服务器 | 时间、天气、难度、游戏规则，常用命令与操作记录 |
| 插件 | 已装插件的启用 / 停用 / 配置 / 删除 |
| 市场 | 插件与整合包商店（含前置依赖），搜索、详情、一键安装 |
| 设置 | 启动配置 + 文件管理（插件文件 / 配置 / 数据 / 日志） |

所有页面均已适配手机浏览器：底部导航 + 抽屉式详情与操作卡片。

## 四、命令身份说明

「消息」页发送框支持多种身份，在输入框上方的选择器切换：

- **聊天**：以面板身份向游戏内发送聊天消息（公告等）。
- **命令（ws）**：通过 WebSocket 向服务器控制台下发指令。
- **ToolDelta 原生**：执行 help / list / reload 等框架控制台命令，输出回到终端日志，不会误发到游戏。

## 五、连接配置说明

- 服务器号 / 密码 / 验证服务器地址均为原版 ToolDelta 方式，请按你使用的启动器（NeOmega / FateArk / TanGame）要求填写；山头服号码为 `mc.163.com/open/mc_realms/?realms=A1234` 中的 `A1234`。
- fbtoken 按原版方式使用：放在运行目录或粘贴到设置页保存；更换验证服务器时记得同步更换 fbtoken。
- GitHub 镜像与插件市场源可在设置页修改，镜像留空时启动会按原版流程自动测速选择。

## 六、目录结构

```
web_panel/                 Web 面板（Flask + WebSocket）
  __init__.py              包标记
  bus.py                   日志总线（fmts.logging → WebSocket 推送）
  bridge.py                框架管理器（后台线程跑 ToolDelta，无 TTY 防护）
  app.py                   Flask 应用（REST + WebSocket + 登录守卫）
  auth_service.py          独立访问账号与凭据生成
  config_service.py        启动配置读写
  plugin_service.py        插件管理 / 市场 / 整合包
  file_service.py          文件管理（白名单虚拟根目录，不暴露源码）
  static/                  index.html / style.css / app.js
web_run.py                 Web 入口（默认打印访问地址与账号）
deploy/docker/             Docker 入口脚本
```

## 七、注意事项

- 原版 CLI 入口 `main.py`（`client_title()` 菜单）仍可用，Web 版走 `web_run.py`，两者互不冲突。
- 日志记录开关、GitHub 镜像、插件市场源均在「设置 → 启动配置」页可改。
- 面板「停止」会安全退出框架（踢机器人 + 保存数据），不会硬杀进程。
- 忘记面板密码：删除 `web_panel_data/credentials.json` 后重启，终端会打印新账号密码。
